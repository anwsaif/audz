// build.gradle
buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:8.7.0' // Adjust version as needed
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
  }
}

